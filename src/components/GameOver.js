// import React from 'react'
// import { connect } from 'react-redux' 
// import { setScore } from './../../redux/actions/scoreActions'

// When game over, I want to render the score form so user can save score to the backend
// Game over will be determined by a function that checks if all cards are flipped
// if all cards are flipped then game over is true and we want to render the score form
// if all cards are not flipped then game is not over and we want don't render score form

class GameOver extends React.Component {
    
//     render(){
//         return (
//             <div>
//                 <h2>All Scores</h2>
//                 <div>
//                     {this.props.score.map(s => <p>{s.username}</p>)}
//                 </div>
//             </div>
//         )
//     }
// }

// import React from 'react';
// import { Switch, Route } from 'react-router-dom';
// import ScoreContainer from './ScoreContainer';

// export default function Score() {
//     return (
//         <Switch>
//             <Route path="/score" component={routerProps => <ScoreContainer routerProps={routerProps} /> } />
//         </Switch>
//     )
}

